# Exclude heavy or host-owned data dirs from build context
node_modules
build
dist
.tmp
.cache
.git

# Docker runtime data/volumes (avoid permission denied during build context)
data/
data/**

# Local configs not needed in build context
config/passwd
config/passwd.tmp

# OS files
.DS_Store
Thumbs.db
